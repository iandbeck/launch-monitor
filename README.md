## Custom Launch Monitor
* Current speed filter set to > 10 mph.
* Outputting raw ADC data

### Modifying Network Configuration 
Run the command: 
`idf.py menuconfig`

Modify the **WiFi SSID** and **WiFi Password** variables. 